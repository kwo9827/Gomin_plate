import React from 'react';

/** 초밥 밑에 깔릴 접시 컴포넌트
 * 1. id에 따라 접시 색이 달라야함
 */
const Plates = ({ id }) => {


    return (
        <div>
            여기에 접시 그리셈
        </div>
    );
};

export default Plates;
